name = "escanor" 
# print(len(name))
# shortname = len(name)
shortname = name[0:3] # it will start from 0 to till 3 but excluded 3rd one
print(shortname)
para = name[1] # this is called string slicing
print("this will print only s: ", para)
